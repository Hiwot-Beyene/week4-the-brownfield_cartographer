[project]
name = "brownfield-cartographer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic==2.12.4",
  "networkx==3.5",
  "tree-sitter",
  "pytest==8.4.2",
  "sqlglot",
  "pyyaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

