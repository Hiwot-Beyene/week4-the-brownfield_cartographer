# Directory for SQLite DB and Chroma vector store.
# Unset: defaults to this repo's .cartography when running the surveyor.
# Set to an absolute or project-relative path to override (e.g. for a shared store).
# CARTOGRAPHER_DATA_DIR=.cartography
